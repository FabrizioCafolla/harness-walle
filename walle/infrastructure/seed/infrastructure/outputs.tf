# Consumer-owned Terraform outputs (walle SEED): created once, never overwritten.
# Expose values produced by this configuration so other modules or CI pipelines can read them.

# Example: output the project name so a deploy script can reference it.
# output "project_name" {
#   description = "The name of the project."
#   value       = var.project_name
# }
