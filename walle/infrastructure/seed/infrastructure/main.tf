# Consumer-owned infrastructure scaffolding (walle SEED): created once, never overwritten by
# `walle update`. Edit freely. Terraform / OpenTofu compatible.

terraform {
  required_version = ">= 1.5.0"
}

# Declare your providers and resources below. Example:
#
# provider "aws" {
#   region = var.region
# }
#
# resource "aws_s3_bucket" "site" {
#   bucket = var.project_name
# }
